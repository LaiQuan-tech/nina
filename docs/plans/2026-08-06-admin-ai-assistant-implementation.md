# 管理後台 AI 小幫手實作計畫

1. 以測試先建立問題驗證、意圖分類與安全報表格式。
2. 建立只讀 Demo 報表資料層，僅組合既有白名單聚合函式。
3. 建立受管理員 session 與 same-origin 保護的 `/api/admin/assistant`。
4. 建立右下角浮動按鈕、對話面板、快速問題、載入與錯誤狀態。
5. 在 `AdminShell` 掛載，補齊響應式、鍵盤與 reduced-motion 樣式。
6. 執行單元測試、production build、API 授權與桌機／手機瀏覽器驗收。
